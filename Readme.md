L'exercice consiste à créer une classe abstraite "Book" dont vont hériter plusieurs classes ("ChildBook", etc...).
La classe Application va appeler les attributs et méthodes via les instances de chaque classe.
